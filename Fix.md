# ix.md — GPS 없는 3D 비행 궤적 시뮬레이션 개선 (헬기 특유 모션 재현, v3)

- 대상 프로젝트: blackbox2 (https://saydals.github.io/blackbox2/ , repo: `saydals/blackbox2`)
- 대상 기능: 3D Replay의 **Without-GPS Estimator** (GPS가 없는 로그에서 콜렉티브 피치 + 기체 자세만으로 비행 궤적을 추정·표시)
- 변경 파일:
  - `src/blackbox-viewer/components/Blackbox3DPanel.vue` (추정기 코어)
  - `src/blackbox-viewer/components/Blackbox3DSettingsDialog.vue` (파라미터 UI)
- 기준 커밋: `4792c7f` (master)
- 검증: ESLint 0 error · 신규 물리 시뮬레이션 스모크 테스트 10/10 통과 · 기존 vitest 결과 변화 없음

---

## 1. 목적

GPS 좌표가 없는 로터리플라이트(RF) 블랙박스 로그로 3D 리플레이를 할 때, 현재 추정기는
"콜렉티브 → 추력, 자세(roll/pitch) → 추력 벡터 회전"이라는 단순 데드레코닝만 수행한다.
이 방식은 물리적으로는 맞지만 **실제 헬기 조종 특유의 모션이 전혀 표현되지 않는다**는 문제가 있다.

실제 헬기(및 그 조종자들)는 다음과 같은 특성을 갖는다.

1. 스틱이 중립 부근에서는 미세한 입력 변화로 고도나 위치가 좌우되지 않는다 (호버 마진).
2. 콜렉티브를 위로 올렸다가 중앙으로 내리면, 로터 관성과 양력 잔존으로 인해
   **일정 시간 떨어지지도 오르지도 않고 공중에 "붙어 있는(hang)"** 느낌이 있으며,
   조종자들은 바로 이 상태에서 여러 기동을 수행한다.
3. 사이클릭을 반대로 넘기면 기체는 즉시 반대 방향으로 가지 않고,
   **강하게 감속되어 한순간 멈춘 뒤("정지 모션") 반대 방향으로 가속**된다.
4. 데드레코닝 특성상 드리프트가 누적되어 경기장(홈포인트) 밖으로 나가는 경우가 많다.

이번 개선은 GPS가 없어도 **위 특성들을 최대한 물리 법칙에 맞게 재현**하여,
콜렉티브/자세 기반 궤적 추정이 실제 비행에 가깝게 보이도록 만드는 것을 목적으로 한다.
(GPS가 있는 로그는 기존과 동일하게 로그 값을 그대로 따르며, 본 변경의 영향이 전혀 없다.)

---

## 2. 기존 구현의 한계 (Current Behavior)

`Blackbox3DPanel.vue`의 `buildFrames()` 내 추정기 블록은 다음과 같이 동작했다.

js

// 기존 코드 (요지)

const coll = row.collective ?? 0;

const aBody = G_ACCEL + (coll - hoverColl) * 0.01 * s.fullPitchAccel; // 콜렉티브 → 추력

const ux/uy/uz = R(yaw,pitch,roll) · [0,0,1]; // 자세로 추력 벡터 회전

const ax = aBody*ux - drag*vx; const ay = aBody*uy - drag*vy;

const az = aBody*uz - G_ACCEL - drag*vz; // 오일러 적분 (50Hz)

// 홈 가드: 반경 100m 하드 클램프 (외향 속도 성분만 제거)

이 구현의 문제점:

| #   | 문제                                                                                      | 결과                                                                      |
| --- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| 1   | 콜렉티브 중앙값(호버 포인트) 근처의 미세 입력도 그대로 추력 변화로 반영                   | 중립 스틱 진동만으로 고도가 계속 들썩임                                   |
| 2   | 상승/하강 중 콜렉티브를 중립으로 내려도 "부유" 개념이 없음                                | 관성으로 계속 상승(balloon)하거나 곧바로 하강 — 헬기의 "정지" 느낌이 없음 |
| 3   | 자세를 반대로 넘기면 추력 벡터만 반대가 되고 속도는 연속적으로 서서히 꺾임                | 실제 헬기의 강한 감속 → 한순간 정지 → 반대 가속 패턴이 재현 안 됨         |
| 4   | 드리프트 제어가 하드 펜스뿐                                                               | 기체가 100m 경계에 붙어서 미끄러지기만 하고, 홈 쪽으로 되돌아오지 않음    |
| 5   | `HOME_LIMIT = 100`                                                                        | 사용자 환경(경기장 크기)에 비해 작음 → 150m 필요                          |
| 6   | (참고) 바로미터 수직 모드(`baroSmooth` 기본값)에서는 추정 고도가 바로미터 값으로 덮어써짐 | 수평 추정에는 개선효과가 적용되지만 수직 "부유"가 화면에 보이지 않음      |

---

## 3. 해결 방법 (요구사항 → 구현 매핑)

### 3.1 요구사항 ① — 콜렉티브 중앙값 ±10 중립 밴드 + 자세 축 중립 밴드

- **콜렉티브**: 호버 포인트 `hoverColl`(자동 캘리브레이션 시 로그 콜렉티브 **중앙값(median)**, 수동 설정 시 사용자 지정값)에서
  `±neutralBand`(기본 10, 콜렉티브 단위) 이내는 **중립 = 순수 호버 추력**으로 본다.

- 소프트 니(soft knee) 방식: 밴드 안에서는 정확히 0, 밴드 밖에서는 `|Δ| - band`만큼만 유효 작용.
  → 밴드 경계에서 추력이 불연속적으로 튀는 것을 방지하면서 "밴드 안=완전 중립"을 보장.

  text

  collDelta = coll - hoverColl

  collEff = sign(collDelta) · max(0, |collDelta| - neutralBand)

  aThrust = G + collEff · 0.01 · fullPitchAccel

- **자세 축(roll/pitch)**: 수평 이동을 유발하는 기울기(tilt)에 대해
  `axisNeutralBand`(기본 8°) 이내의 기울기는 **추력 벡터가 연직 방향인 것으로 취급**해
  수평 이동을 발생시키지 않는다. 콜렉티브와 동일한 소프트 니 적용.
  **화면에 표시되는 기체 자세는 로그 값 그대로**이며, 번역(이동) 입력에만 밴드를 적용한다.

  text

  tiltMag = hypot(ux, uy) // sin(tilt) 성분

  tiltNeutral= sin(axisNeutralBand)

  tiltEff = max(0, tiltMag - tiltNeutral) // 밴드 차감 (소프트 니)

  uxT,uyT = (ux,uy)/tiltMag · tiltEff // 유효 수평 추력 방향

  uzT = sqrt(1 - tiltEff²) // 유효 연직 추력

### 3.2 요구사항 ② — 중립 전환 시 중력 감쇠 "부유(hang)" 처리

- **트리거**: 콜렉티브가 밴드 밖(상승/하강 중)에서 **밴드 안으로 들어오는 순간**,
  수직 속도가 `FLOAT_VZ_TRIGGER`(0.4 m/s)보다 크면 `floatTime`(기본 2초) 동안의 부유 창을 연다.
- **효과**: 부유 창 동안 중력 완화율 `relief`가
  `gravityRelief`(기본 0.7) × (남은 시간 비율, 선형 감쇠 1→0)로 적용되어
  ① 순수직 가속도 `az`가 `(1 - relief)`배로 감쇠하고(중력·추력 과잉 모두 소멸),
  ② 수직 속도가 매 프레임 `vz -= vz·min(0.9, relief·8·dt)`로 블리딩되어 0으로 수렴한다.
- 결과: 상승 직후 중립 → 관성 coast 후 멈춤, 하강 직후 중립 → 낙하가 한순간 멈춤.
  창이 닫히면 relief가 0으로 연속 감쇠하므로 일반 물리로 부드럽게 복귀한다.
  중립 유지 시 창 종료 후에도 `aThrust = G`로 등가 호버 상태(정지 유지)가 되고,
  밴드 아래로 내리면 자연스럽게 가라앉는다 — "콜렉티브 중앙에서 기동한다"는 사용 시나리오와 일치.
- **바로미터 모드 연동**: 기본 수직 소스가 `baroSmooth`이므로, 부유 창 동안에는
  표시 고도를 "전환 순간 고도(`floatAltHold`)"쪽으로 `relief` 비율만큼 블렌딩해
  부유 모션이 바로미터 모드에서도 화면에 보이도록 함. 창 종료 시 연속 해제.

### 3.3 요구사항 ③ — 자세 반대 전환 시 정지(settle) 모션

- **트리거**: 수평 속도 `hSpeed > REVERSE_SPEED_TRIGGER`(1 m/s)이고,
  현재 유효 기울기 벡터 `(uxT,uyT)`와 현재 속도 벡터의 코사인 `dirDot < -0.25`
  (즉, 추력 방향이 현재 이동 방향의 반대)이면 `reversePause`(기본 0.6초) 정지 창을 연다.
- **효과**: 정지 창 동안 수평 항력을 `drag + SETTLE_DRAG_MAX(6/s)·(남은 시간 비율)`로
  강화(선형 감쇠)한다. 기체는 급감속 → 한순간 정지(벌크업) → 반대 방향 가속의
  실제 헬기 패턴을 따르게 된다. 감쇠가 지수적으로 풀리므로 모션이 부자연스럽지 않다.

### 3.4 요구사항 ④ — 홈(출발지) 방향 드리프트 제어

- 펜스 안쪽에 **소프트 반경** `homeSoftRadius × HOME_LIMIT`(기본 0.6 → 90m)을 두고,
  그 밖으로 나가면 홈 방향으로 `homeBias·w` (기본 최대 1.0 m/s²,
  `w`는 소프트 반경→펜스에서 0→1로 선형 증가)의 되돌림 가속을 부여한다.
- 중립 자세의 순수 드리프트는 소프트 반경 밖에서 점차 홈으로 휘어져 되돌아오고,
  의도적인 조종(지속 기울기)에는 펜스가 백스톱으로 동작한다.

### 3.5 요구사항 ⑤ — 홈 가드 100m → 150m

- `HOME_LIMIT = 100` → **`150`**. 기존의 "경계에서 외향 속도 성분만 제거(경계를 따라 미끄러짐)"
  방식은 그대로 유지된다. HUD의 "Dist to home" 표시와도 일관된다.

### 3.6 요구사항 ⑥ — 물리 법칙 정합성

- 기존에 적용된 물리(추력 벡터의 자세 기반 회전, 중력, 선형 항력, 50Hz 오일러 적분, 지면 클램프,
  프레임 간 변위로부터 지상속도 산출)는 모두 유지되며, 신규 로직은 전부 힘/가속도 레벨에서
  추가되므로 물리적으로 일관된다 (순간이동/하드코드 포즈 없음).
- 소프트 니(deadzone)는 밴드 경계에서 추력·기울기를 C0 연속으로 만들어
  프레임 간 튀는 현상(jitter)을 제거했다.
- GPS가 있는 로그는 `useEstimator = !hasGps` 분기에 의해 기존 경로(로그 값 그대로)를 그대로 사용한다.

---

## 4. 해결 diff

적용: 저장소 루트에서 `diff.txt` (아래 전체 diff가 곧 패치 파일 내용과 동일)
첨부 패치 파일: `diff.txt` (fix.md와 같은 디렉터리)



## 5. 신규 파라미터 (튜닝 가이드)

3D Replay → "No GPS" 버튼 → 설정 다이얼로그에서 조정 가능. 모두 ConfigStorage에 저장됨(키 버전 v3로 bump되어 기존 저장값과 충돌 없음).

| 파라미터          | 기본값  | 단위          | 의미 / 튜닝 포인트                                                         |
| ----------------- | ------- | ------------- | -------------------------------------------------------------------------- |
| `neutralBand`     | **10**  | 콜렉티브 단위 | 호버 중심 ±이 범위는 완전 중립. "중립을 더 넓게" → 12~15                   |
| `axisNeutralBand` | **8**   | deg           | 이 기울기 이하는 수평 이동 없음. 자세 밴드("다른 축 중립")                 |
| `gravityRelief`   | **0.7** | 0~1           | 부유 시작 시 억제 강도. 1.0=완전 고정, 낮추면 부유가 약해짐                |
| `floatTime`       | **2.0** | s             | 콜렉티브 중립 복귀 후 부유 지속 시간                                       |
| `reversePause`    | **0.6** | s             | 사이클릭 역전 후 강한 감속(정지 모션) 지속 시간                            |
| `homeBias`        | **1.0** | m/s²          | 소프트 반경 밖에서 홈으로 되돌리는 최대 가속. 크게 하면 더 적극적으로 복귀 |
| `homeSoftRadius`  | **0.6** | × HOME_LIMIT  | 드리프트 제어 시작 지점 (0.6 → 90m)                                        |
| `HOME_LIMIT`      | **150** | m             | 하드 펜스 (소스 상수, 100 → 150 변경)                                      |

기존 파라미터(`verticalSource`, `baroSmoothing`, `autoHover`, `hoverCollective`, `fullPitchAccel`, `drag`, `startAltitude`)는 변경 없음.

> 참고: `hoverColl`(중앙값)은 `autoHover=true`(기본)일 때 로그 콜렉티브의 **median**으로 자동 캘리브레이션되며,
> 요구사항 ①의 "콜렉티브피치 중앙값 ±10"은 이 중앙값 기준 밴드로 구현되어 있다.

---

## 6. 검증 결과

### 6.1 추정기 물리 스모크 테스트 (신규 로직 동등 복제, 50Hz)



diff.txt  



## 5. 신규 파라미터 (튜닝 가이드)

3D Replay → "No GPS" 버튼 → 설정 다이얼로그에서 조정 가능. 모두 ConfigStorage에 저장됨(키 버전을 v3로 bump하여 기존 저장값과 충돌 없음).

| 파라미터          | 기본값  | 단위          | 의미 / 튜닝 포인트                                                         |
| ----------------- | ------- | ------------- | -------------------------------------------------------------------------- |
| `neutralBand`     | **10**  | 콜렉티브 단위 | 호버 중심 ±이 범위는 완전 중립. "중립을 더 넓게" → 12~15                   |
| `axisNeutralBand` | **8**   | deg           | 이 기울기 이하는 수평 이동 없음. 자세 축의 중립 밴드                       |
| `gravityRelief`   | **0.7** | 0~1           | 부유 시작 시 억제 강도. 1.0=완전 고정, 낮추면 부유가 약해짐                |
| `floatTime`       | **2.0** | s             | 콜렉티브 중립 복귀 후 부유 지속 시간                                       |
| `reversePause`    | **0.6** | s             | 사이클릭 역전 후 강한 감속(정지 모션) 지속 시간                            |
| `homeBias`        | **1.0** | m/s²          | 소프트 반경 밖에서 홈으로 되돌리는 최대 가속. 크게 하면 더 적극적으로 복귀 |
| `homeSoftRadius`  | **0.6** | × HOME_LIMIT  | 드리프트 제어 시작 지점 (0.6 → 90m)                                        |
| `HOME_LIMIT`      | **150** | m             | 하드 펜스 (소스 상수, 100 → 150 변경)                                      |

기존 파라미터(`verticalSource`, `baroSmoothing`, `autoHover`, `hoverCollective`, `fullPitchAccel`, `drag`, `startAltitude`)는 변경 없음.

> 참고: `hoverColl`(중앙값)은 `autoHover=true`(기본)일 때 로그 콜렉티브의 **median**으로 자동 캘리브레이션되며,
> 요구사항 ①의 "콜렉티브피치 중앙값 ±10"은 이 중앙값 기준 밴드로 구현되어 있다.

---

## 6. 검증 결과

### 6.1 추정기 물리 스모크 테스트 (신규 로직 동등 복제, 50Hz)

text

PASS R1 중립 밴드: 콜렉티브 지터 ±10 → 고도 변화 0.000 m (완전 중립)

PASS R2 부유: 상승 6.73 m/s → 중립 전환 1초 내 vz 0.72 m/s (15% 미만)

PASS R2 부유: 부유 중 고도 변화 2.35 m (관성 coast 포함, 실제 헬기와 동일 수준)

PASS R2 부유: 중립 유지 시 기체 정지 상태 유지 (|vz| 0.3 미만)

PASS R2 부유: 밴드 아래(-15)로 내리면 정상 하강 재개 (vz -0.46 m/s)

PASS R3 정지 모션: 전진 5.99 m/s → 역전 기울기 0.6초 후 0.22 m/s (25% 미만)

PASS R5 펜스: 지속 25도 기울기 60초 비행에서도 150 m 초과 없음

PASS R5 펜스: 15도 완만 비행도 150 m 이내

PASS R4 드리프트 제어: 중립 자세 드리프트가 150 m → 79.6 m로 홈 방향 복귀

PASS R6 물리: 전 시나리오에서 상태 유한 (NaN 없음)

### 6.2 정적 검증

- `eslint` (두 변경 파일): **0 error / 0 warning**
- `vitest run`: 44 passed / 4 failed — 실패 4건은 `rf_labels` 계열로 **변경 전 master에서도 동일하게 실패하는 기존 항목** (본 변경과 무관, stash 비교로 확인)
- `git apply --check`: 클린 트리에 패치가 깨끗하게 적용됨

---

## 7. 적용 / 롤백 방법

bash

# 저장소 루트에서

git apply v3_estimator.diff # 적용

git apply -R v3_estimator.diff # 롤백

또는 위 diff 내용을 두 파일에 수동 반영해도 된다. 적용 후 `npm run build`(또는 dev 서버)로
확인하며, GPS 없는 .bbl 로그를 열어 3D Replay → 설정(No GPS)에서 신규 항목
(Neutral bands / Hang / Cyclic reversal settle / Drift control)이 노출되는지 확인한다.

확인 포인트:

1. 콜렉티브를 중앙 부근에 두면 고도가 고정된다 (중립 밴드 + 호버 추력).
2. 상승 → 콜렉티브 중앙: 기체가 잠시 붙어 있다가(부유) 밴드 아래로 내리면 가라앉는다.
3. 전진 비행 중 사이클릭 역전: 급감속 후 한순간 멈춤 뒤 반대 방향으로 가속한다.
4. 오래 떠 있어도 홈 150 m 안에 머무르며, 소프트 반경 밖에서는 서서히 홈 쪽으로 되돌아온다.
5. GPS 있는 로그는 기존과 100% 동일하게 재생된다.

---

## 8. 부록 — 해결 diff 전문 (`v3_estimator.diff`)

> 이 문서의 변경 내용 전체를 원문 그대로 옮긴 통합 diff 이다. 내용을 파일로 저장하면 `git apply v3_estimator.diff` 로 그대로 적용할 수 있다. diff.txt 로 대신한다. v3_estimator.diff는 존재하지 않음.
